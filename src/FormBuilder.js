/**
 * @overview 构建表单职责申明
 * @author shiwei.lv
 */

export default class FormBuilder {
  constructor(schemaDescription, options) {
    this.schemaDescription = schemaDescription;
    this.options = options;
  }

  build() {
    return;
  }
}
