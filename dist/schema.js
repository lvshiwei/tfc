/**
 * @overview A Yup schema generated by tfc.
 */
import { object, mixed, date, string, number, array } from "yup";
export default object({
  expandProjectNo: string().label("expandProjectNo"),
  shopLocation: string().max(30).label("门店地址"),
  preLongitude: number().label("经度"),
  preLatitude: number("请填写正确的纬度").max(1, "不能太大").min(0).label("纬度"),
  preOpeningTime: date().max(NaN, "不能太晚").required("请填写预开业日期").label("预开业时间"),
  isSupportCanteen: number().required("请选择是否支持堂食").label("支持堂食"),
  seatsNumber: number().min(1, "请填写正确的座位数").required("请填写座位数").label("座位数"),
  active: bool().required("请选择是否生效").label("生效的")
});